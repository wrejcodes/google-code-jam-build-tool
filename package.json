{
  "name": "gcj",
  "version": "1.0.0",
  "description": "google code jam transpiler/minifier",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "new": "node commands.js new --name",
    "exec": "node commands.js run --name",
    "build": "node_modules/.bin/gulp build --src"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/preset-env": "^7.4.3",
    "gulp": "^4.0.0",
    "gulp-babel": "^8.0.0",
    "gulp-babel-minify": "^0.5.0",
    "gulp-rename": "^1.4.0"
  }
}
